# Local .terraform directories
**/.terraform/*

# .tfstate files and backups
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data.
# Most .tfvars files should be managed securely out of version control.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local testing
# and not checked in to the repository.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore plan output files (if you use this convention)
# example: *tfplan*
